Rebuild_Ireland = {
	Industrial_Ireland = {
        visible = { original_tag = "IRE"
					has_completed_focus = IRE_industrial_buildup }
		icon = generic_major_decision
		cost = 100
		days_remove = 30
		complete_effect = {
			random_owned_state = {
				add_extra_state_shared_building_slots = 2
			}
		}
	}
	Get_Irish_Foreign_Support = {
        visible = { original_tag = "IRE" 
					has_completed_focus = IRE_foreign_support }
		icon = generic_major_decision
		cost = 100
		days_remove = 180
		complete_effect = {
			IRE = {
				add_offsite_building = {
					type  = industrial_complex
					level = 2
				}
			}
		}
	}
	IRA_Foreign_Supply = {
        visible = { original_tag = "IRE" 
					OR = {	has_idea = PIRA_Military_Government
							has_idea = PIRA_Friendly
						}
					}
		icon = generic_major_decision
		cost = 200
		days_remove = 365
		complete_effect = {
			IRE = {
				add_offsite_building = {
					type  = arms_factory
					level = 2
				}
			}
		}
	}
	Celtic_Zionism = {
        visible = { original_tag = "IRE" has_completed_focus = IRE_aggressive_irish_migration }
        icon = generic_major_decision
        days_remove = 30
        cost = 50
        complete_effect = {
			if = {
				limit = { has_idea = IRE_zionism }
				every_state = {
					limit = {is_controlled_by = IRE }
					add_manpower = 50000
				}
			} 
			if = {
				limit = {has_idea = IRE_migrant_effort}
				random_core_state = { add_manpower = 50000 }
			}
    	}
	}
}